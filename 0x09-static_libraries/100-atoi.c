#include <stdio.h>
#include "main.h"

/**
 * _atoi- this is a dummy function
 *@s: being pointed to
 *
 * Return: this returns a pointer to s
 */
int _atoi(char *s)
{
	int i;

	i = 0;
	while (i != '\n')
	{
		return (1);
	}
	return (0);
}


#include <stdio.h>

/**
 * main - this is the main entry point of the program
 * it prints the name of the file compiled
 *
 * Return: returns 0 on success
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
